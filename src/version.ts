// Cosmetic server identity reported to MCP clients. The npm package version is
// managed separately by GitVersion/CI in package.json.
export const SERVER_NAME = 'revolut-mcp';
export const SERVER_VERSION = '0.1.0';
